export const FINANCIAL_SCREENER_DESCRIPTION = `Run a screener over financial metrics using structured filters.

Use to build a ticker universe based on metric thresholds (market cap, EV/EBITDA, ROIC, etc.).
Returns tickers only (max 100). Results are cached for 12 hours.`;
